# Frontend placeholder

The main repository README lists the planned `src` layout (including `reactor/`) but no frontend source files are present in the repository yet. This placeholder clarifies that the frontend implementation has not been committed. If you add the frontend, place the `src/` tree here (for example `src/reactor/` with `createReactor.tsx`, `router.tsx`, etc.).
